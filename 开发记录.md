# 贪吃蛇游戏开发全流程记录

## 概述

本文记录了如何使用 OpenClaw + OpenCode 开发一个贪吃蛇游戏，并部署到 GitHub 和 Vercel 的完整过程。

---

## 环境准备

### 1. 安装 OpenCode

```bash
npm install opencode-ai -g
opencode --version
# 输出: 1.2.25
```

### 2. 创建工作目录

```bash
mkdir -p /workspace/myopencode
cd /workspace/myopencode
```

### 3. 启动 OpenCode（使用免费模型 Big Pickle）

```bash
opencode -m opencode/big-pickle
```

> 注意：OpenCode 内置免费模型 `opencode/big-pickle`，无需 API Key 即可使用。

---

## 游戏开发

### 1. 编写贪吃蛇游戏代码

由于 OpenCode 免费模型响应较慢，我们直接编写了完整的 HTML + JavaScript + Tailwind CSS 游戏：

**文件：** `/workspace/myopencode/snake.html`

**功能特性：**
- ✅ 经典贪吃蛇玩法
- ✅ 分数实时显示
- ✅ 游戏结束显示最终分数
- ✅ 可以重新开始
- ✅ 使用 Tailwind CSS 样式
- ✅ 键盘方向键控制

### 2. 核心代码结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>贪吃蛇游戏</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <!-- 游戏画布 -->
    <canvas id="gameCanvas" width="400" height="400"></canvas>
    
    <script>
        // 游戏逻辑...
    </script>
</body>
</html>
```

---

## GitHub 部署

### 1. 初始化 Git 仓库

```bash
cd /workspace/myopencode
git init
git config user.email "your-email@qq.com"
git config user.name "your-username"
```

### 2. 添加文件并提交

```bash
git add -A
git commit -m "Add snake game"
```

### 3. 创建 GitHub 仓库（使用 API）

```bash
curl -X POST \
  -H "Authorization: token YOUR_GITHUB_TOKEN" \
  -H "Accept: application/vnd.github.v3+json" \
  https://api.github.com/user/repos \
  -d '{"name":"snake-game","description":"A classic Snake game","private":false}'
```

### 4. 推送代码到 GitHub

```bash
git remote add origin https://YOUR_TOKEN@github.com/your-username/snake-game.git
git branch -M main
git push -u origin main
```

**仓库地址：** https://github.com/it577net/snake-game

---

## Vercel 部署

### 1. 安装 Vercel CLI

```bash
npm install -g vercel
```

### 2. 部署到 Vercel

```bash
cd /workspace/myopencode
vercel --prod --yes --token YOUR_VERCEL_TOKEN
```

**部署成功输出：**
```
Production: https://your-project.vercel.app [7s]
Aliased: https://your-project-name.vercel.app [7s]
```

**游戏链接：** https://myopencode-mocha.vercel.app/snake.html

---

## 访问方式

部署完成后，可以通过以下方式访问：

1. **直接访问（需加文件名）：**
   ```
   https://myopencode-mocha.vercel.app/snake.html
   ```

2. **（可选）将 snake.html 改名为 index.html：**
   ```bash
   mv snake.html index.html
   ```
   然后直接访问：`https://myopencode-mocha.vercel.app`

---

## 使用的工具

| 工具 | 用途 |
|------|------|
| OpenCode | AI 编程助手 |
| Big Pickle | OpenCode 免费模型 |
| GitHub API | 创建仓库、推送代码 |
| Vercel | 静态网站托管部署 |
| Tailwind CSS | 前端样式框架 |

---

## 注意事项

1. **GitHub Token** 需要勾选 `repo` 权限
2. **Vercel Token** 需要从 Vercel Dashboard 获取
3. 静态网页部署建议将入口文件命名为 `index.html`
4. OpenCode 免费模型响应可能较慢，适合简单任务

---

## 最终成果

- **GitHub 仓库：** https://github.com/it577net/snake-game
- **在线游戏：** https://myopencode-mocha.vercel.app/snake.html
